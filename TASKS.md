# TASKS.md

## 목적

이 문서는 `Cherry Wish` 프로젝트를 구현하기 위한 작업 순서와 완료 기준을 정의한다.
Codex는 이 문서를 기준으로 작업을 단계적으로 수행한다.

이 문서는 반드시 아래 문서들과 함께 사용한다.

* `CHERRY_WISH_SPEC.md`
* `AGENTS.md`

`CHERRY_WISH_SPEC.md`는 무엇을 만들지 정의하고,
`AGENTS.md`는 어떤 기준으로 구현할지 정의하며,
`TASKS.md`는 어떤 순서로 구현할지 정의한다.

---

## 전체 원칙

### 최우선 목표

가장 중요한 것은 아래 핵심 경험을 빠르고 부드럽게 완성하는 것이다.

**나무를 누른다 -> 꽃잎이 떨어진다 -> 꽃잎을 잡는다 -> 소원 메시지를 받는다**

### 작업 원칙

* P0부터 순서대로 구현한다
* 아직 필요하지 않은 고급 추상화는 만들지 않는다
* 각 단계는 눈에 보이는 결과를 남겨야 한다
* 각 Task는 독립적으로 검증 가능해야 한다
* 성능보다 기능, 기능보다 구조가 아니라, **핵심 UX 완성도**를 우선한다
* 모바일 기준에서 먼저 판단한다

### 금지사항

* 명세 범위를 벗어난 기능 추가 금지
* 3D 확장 금지
* 인증/저장/API 연동 금지
* 과한 게임화 금지
* 디자인 시스템 구축처럼 범위를 불필요하게 확장하지 말 것

---

## 마일스톤 구조

* Milestone 1: 기본 화면과 상태 구조 완성
* Milestone 2: 핵심 인터랙션 완성
* Milestone 3: 결과 경험 완성
* Milestone 4: 품질 보강 및 마감

---

# Milestone 1. 기본 화면과 상태 구조 완성

## Task 1. 프로젝트 기본 구조 세팅

### 목표

프로젝트가 구현 가능한 기본 상태가 되도록 구조를 만든다.

### 작업 내용

* React 또는 Vue + TypeScript 기반 프로젝트 준비
* PixiJS, GSAP 설치
* 기본 디렉터리 구조 생성
* entry page 생성
* 글로벌 스타일 또는 기본 reset 준비

### 산출물

* 실행 가능한 기본 프로젝트
* 빈 페이지가 아니라 기본 레이아웃이 보이는 상태

### 완료 기준

* 프로젝트가 오류 없이 실행된다
* 루트 페이지 진입이 가능하다
* 기본 폴더 구조가 정리되어 있다

### 의존성

* 없음

---

## Task 2. 페이지 레이아웃 뼈대 구현

### 목표

상단/중앙/하단/모달 영역이 있는 단일 페이지 골격을 만든다.

### 작업 내용

* `CherryWishPage` 컴포넌트 생성
* 상단 타이틀/설명 배치
* 중앙 메인 무대 영역 생성
* 하단 안내 문구 영역 배치
* 결과 모달 자리 구조 생성

### 산출물

* 정적 상태의 한 화면 레이아웃

### 완료 기준

* 모바일 기준 레이아웃이 자연스럽다
* 중앙 영역이 메인 인터랙션 무대로 비어 있다
* 타이틀/설명/안내 문구가 보인다

### 의존성

* Task 1

---

## Task 3. 상태 머신 및 공용 설정값 정의

### 목표

전체 흐름을 제어하는 상태 구조와 설정값 상수를 만든다.

### 작업 내용

* `SceneState` 타입 정의
* 초기 상태 `idle` 구성
* `sceneConfig.ts` 생성
* 꽃잎 수, 중력, 흔들림 시간 등 설정값 상수화
* 랜덤 메시지 상수 파일 생성

### 산출물

* 상태 타입
* 설정 상수 파일
* 메시지 상수 파일

### 완료 기준

* 상태와 설정값이 컴포넌트 밖에서 관리된다
* 하드코딩 숫자가 최소화된다

### 의존성

* Task 1

---

# Milestone 2. 핵심 인터랙션 완성

## Task 4. 배경과 idle 상태 연출 구현

### 목표

페이지 진입 시 화면이 살아 있는 느낌을 만든다.

### 작업 내용

* `SpringBackground` 컴포넌트 생성
* 배경색/빛/장식 요소 배치
* idle 상태에서 미세한 배경 움직임 구현
* 배경 꽃잎 2~3개 정도의 느린 움직임 추가

### 산출물

* 정적이지 않은 배경

### 완료 기준

* 페이지 진입 시 화면이 멈춰 보이지 않는다
* 배경이 과하지 않고 차분하다

### 의존성

* Task 2
* Task 3

---

## Task 5. 벚꽃나무 렌더링 및 클릭 영역 구현

### 목표

중앙에 나무를 배치하고, 사용자가 클릭할 수 있는 상태로 만든다.

### 작업 내용

* `WishTreeScene` 컴포넌트 생성
* 나무 SVG 또는 이미지 배치
* 클릭 가능한 캐노피 영역 정의
* hover 또는 터치 가능한 오브젝트라는 힌트 추가 가능

### 산출물

* 화면 중앙의 나무
* 클릭 가능한 나무 영역

### 완료 기준

* 나무가 중앙 무대에 자연스럽게 배치된다
* 클릭/터치가 정상 인식된다

### 의존성

* Task 2
* Task 3

---

## Task 6. 나무 흔들림 애니메이션 구현

### 목표

나무를 클릭하면 즉시 반응하도록 만든다.

### 작업 내용

* GSAP 기반 흔들림 구현
* 클릭 시 상태를 `shaking`으로 전환
* 0.8초~1.2초 사이의 자연스러운 흔들림 구성
* 과도한 흔들림 방지

### 산출물

* 나무 클릭 시 반응 애니메이션

### 완료 기준

* 클릭 직후 바로 시각 반응이 나온다
* 흔들림이 부드럽고 과하지 않다

### 의존성

* Task 5

---

## Task 7. 꽃잎 데이터 모델 및 엔진 구현

### 목표

꽃잎 객체를 생성하고 프레임 단위로 업데이트하는 엔진을 만든다.

### 작업 내용

* `petalTypes.ts` 생성
* `usePetalEngine.ts` 생성
* 꽃잎 객체 생성 함수 구현
* 위치/속도/회전/드리프트 업데이트 로직 구현
* 화면 밖 제거 처리 구현

### 산출물

* 꽃잎 시뮬레이션 로직

### 완료 기준

* 꽃잎 객체가 생성/업데이트/제거된다
* 직접 좌표 계산으로 동작한다
* 불필요한 물리엔진 의존 없이 작동한다

### 의존성

* Task 3

---

## Task 8. 꽃잎 렌더링 및 낙하 연출 구현

### 목표

나무 클릭 후 꽃잎이 살랑살랑 떨어지는 दृश्य을 완성한다.

### 작업 내용

* `PetalSystem` 컴포넌트 생성
* 나무 클릭 후 꽃잎 생성 연결
* 꽃잎 렌더링 구현
* 중력, 드리프트, 회전 시각화
* 모바일/데스크탑 꽃잎 수 분기 적용

### 산출물

* 실제로 떨어지는 꽃잎 인터랙션

### 완료 기준

* 나무 클릭 후 꽃잎이 보인다
* 꽃잎이 자연스럽게 떨어진다
* 모바일에서 과도하게 무겁지 않다

### 의존성

* Task 6
* Task 7

---

## Task 9. 소원 꽃잎 선택 및 포착 판정 구현

### 목표

떨어지는 꽃잎 중 하나를 잡으면 성공 처리되도록 만든다.

### 작업 내용

* 꽃잎 중 1개를 `wishPetal`로 지정
* 클릭/터치 hit test 구현
* 첫 플레이에서는 더 쉬운 판정 적용
* 일반 꽃잎과 소원 꽃잎 판정 분리
* 포착 시 상태를 `petalCaught`로 전환

### 산출물

* 꽃잎 포착 가능 상태

### 완료 기준

* 사용자가 실제로 꽃잎을 잡을 수 있다
* 첫 플레이 실패 확률이 낮다
* 판정이 너무 빡빡하지 않다

### 의존성

* Task 8

---

# Milestone 3. 결과 경험 완성

## Task 10. 결과 모달 구현

### 목표

꽃잎을 잡았을 때 기분 좋은 결과 경험을 완성한다.

### 작업 내용

* `WishResultModal` 컴포넌트 생성
* 랜덤 메시지 출력 연결
* 모달 등장 애니메이션 구현
* 타이틀, 메시지, 버튼 UI 구성

### 산출물

* 결과 모달

### 완료 기준

* 꽃잎 포착 후 자연스럽게 결과가 뜬다
* 메시지가 랜덤으로 잘 표시된다
* 모달 디자인이 과하지 않다

### 의존성

* Task 3
* Task 9

---

## Task 11. 다시 하기 루프 구현

### 목표

한 번 체험 후 다시 같은 흐름을 반복할 수 있게 만든다.

### 작업 내용

* `resetting` 상태 구현
* 꽃잎 제거 및 상태 초기화
* 모달 닫기 처리
* 다시 하기 버튼 연결
* 재플레이 시 동일 흐름 재진입 보장

### 산출물

* 반복 가능한 인터랙션 루프

### 완료 기준

* 다시 하기 버튼으로 정상 재시작된다
* 이전 상태가 남지 않는다
* 두 번째 플레이 이후도 안정적으로 동작한다

### 의존성

* Task 10

---

## Task 12. 안내 문구 및 상태별 UI 힌트 구현

### 목표

사용자가 지금 무엇을 해야 하는지 자연스럽게 이해하게 만든다.

### 작업 내용

* `InstructionOverlay` 컴포넌트 생성
* idle / petalsFalling / resultOpen 등에 따라 문구 변화
* 불필요하게 많은 설명은 배제

### 산출물

* 상태별 안내 문구 UI

### 완료 기준

* 처음 보는 사용자도 흐름을 이해할 수 있다
* 문구가 과하지 않고 명확하다

### 의존성

* Task 3
* Task 10

---

# Milestone 4. 품질 보강 및 마감

## Task 13. 반응형 및 모바일 터치 UX 최적화

### 목표

모바일에서 핵심 경험이 매끄럽게 동작하게 만든다.

### 작업 내용

* 클릭 타깃 크기 조정
* 화면 비율에 따른 나무/모달/문구 위치 조정
* 모바일 꽃잎 수 제한 확인
* 터치 판정 튜닝

### 산출물

* 모바일 최적화된 화면

### 완료 기준

* 모바일 세로 화면에서 레이아웃이 깨지지 않는다
* 터치가 쉽다
* 꽃잎을 잡는 경험이 답답하지 않다

### 의존성

* Task 11
* Task 12

---

## Task 14. 성능 튜닝 및 애니메이션 정리

### 목표

부드러운 체감 품질을 확보한다.

### 작업 내용

* requestAnimationFrame 루프 점검
* inactive 객체 정리 점검
* 불필요한 리렌더 최소화
* 꽃잎 수, 속도, 드리프트 값 미세 조정
* 애니메이션 과장 제거

### 산출물

* 더 안정적인 최종 인터랙션

### 완료 기준

* 모바일에서 프레임 드랍이 심하지 않다
* 꽃잎 움직임이 자연스럽다
* 전체 경험이 가볍다

### 의존성

* Task 13

---

## Task 15. 사운드 기능 추가

### 목표

필수는 아니지만 있으면 좋은 감성 요소를 추가한다.

### 작업 내용

* `SoundControl` 컴포넌트 생성
* 클릭 효과음 연결
* 꽃잎 포착 효과음 연결
* 음소거 토글 구현

### 산출물

* 선택 가능한 사운드 기능

### 완료 기준

* 사운드 없이도 기능은 성립한다
* 사운드가 있어도 과하지 않다
* 모바일에서 갑작스럽지 않다

### 의존성

* Task 11

---

## Task 16. 공유하기 기능 추가

### 목표

체험 이후 재확산 포인트를 만든다.

### 작업 내용

* 결과 모달에 공유하기 버튼 추가
* Web Share API 우선 검토
* 미지원 환경은 복사 fallback 가능

### 산출물

* 기본 공유 기능

### 완료 기준

* 공유 버튼이 정상 동작한다
* 공유 기능이 없어도 핵심 UX는 깨지지 않는다

### 의존성

* Task 10

---

## Task 17. 코드 정리 및 마감

### 목표

최종 코드를 읽기 쉽고 유지보수 가능한 상태로 마감한다.

### 작업 내용

* 불필요한 코드 제거
* 타입/상수/유틸 정리
* 네이밍 정리
* 파일 구조 점검
* 주석 최소 보완

### 산출물

* 정리된 최종 코드베이스

### 완료 기준

* 하드코딩이 줄어들었다
* 컴포넌트 책임이 명확하다
* 코드 흐름을 읽기 쉽다

### 의존성

* Task 14
* Task 15
* Task 16

---

# 최종 검수 체크리스트

## 핵심 UX 검수

* [ ] 진입 직후 화면이 살아 있다
* [ ] 나무 클릭 시 즉시 반응한다
* [ ] 꽃잎이 자연스럽게 떨어진다
* [ ] 꽃잎을 잡는 경험이 직관적이다
* [ ] 결과 메시지가 기분 좋게 뜬다
* [ ] 다시 하기 루프가 안정적이다

## 구현 품질 검수

* [ ] 모바일에서 레이아웃이 자연스럽다
* [ ] 상태 전이가 꼬이지 않는다
* [ ] 설정값이 상수로 분리되어 있다
* [ ] 타입이 명확하다
* [ ] 성능이 과도하게 무겁지 않다

## 디자인 검수

* [ ] 과한 핑크 사용이 없다
* [ ] 과한 애니메이션이 없다
* [ ] 화면이 촌스럽지 않고 차분하다
* [ ] 타이포와 여백이 안정적이다

---

# Codex 실행 원칙

Codex는 반드시 아래 순서로 접근한다.

1. P0에 해당하는 Task부터 구현한다
2. 각 Task 완료 후 실행 가능한 상태를 유지한다
3. 다음 Task로 넘어가기 전에 현재 Task의 완료 기준을 만족하는지 확인한다
4. 기능 추가보다 핵심 UX 완성도를 우선한다
5. 구조가 지나치게 무거워지면 더 단순한 방식으로 되돌린다

이 프로젝트의 성공 기준은 기능 수가 아니라, 아래 흐름의 완성도다.

**나무를 누른다 -> 꽃잎이 떨어진다 -> 꽃잎을 잡는다 -> 소원 메시지를 받는다**

이 경험이 짧고, 부드럽고, 예쁘고, 직관적이면 성공이다.
