# AGENTS.md

## 목적

이 문서는 Codex 또는 다른 코드 에이전트가 `Cherry Wish` 프로젝트를 일관된 기준으로 구현하도록 돕는 작업 운영 문서다.
이 문서는 무엇을 만들지 설명하는 PRD가 아니라, **어떻게 일하고 어떤 기준으로 구현할지**를 정의한다.

반드시 `CHERRY_WISH_SPEC.md`를 함께 읽고, 그 명세를 기준으로 구현한다.

---

## 프로젝트 컨텍스트

### 프로젝트 이름

Cherry Wish

### 제품 정의

벚꽃나무를 클릭하면 꽃잎이 떨어지고, 사용자가 꽃잎을 잡으면 오늘의 소원 메시지를 보여주는 단일 화면 인터랙티브 이벤트 페이지.

### 핵심 UX

사용자는 설명을 길게 읽지 않아도 다음 흐름을 즉시 이해할 수 있어야 한다.

1. 나무를 본다
2. 나무를 누른다
3. 꽃잎이 떨어진다
4. 꽃잎을 잡는다
5. 소원 메시지를 받는다

### 핵심 원칙

* 단일 페이지 완성도가 가장 중요하다
* 짧고 감성적인 인터랙션이 목적이다
* 물리 정확도보다 시각적 자연스러움이 우선이다
* 첫 플레이는 거의 반드시 성공해야 한다
* 모바일에서의 체감 품질을 우선한다

---

## 에이전트의 역할

에이전트는 단순히 코드를 생성하는 역할이 아니라, 다음 기준을 만족하는 구현 파트너로 동작해야 한다.

### 해야 하는 일

* 명세를 읽고 UI/인터랙션/구조를 구현한다
* 지나친 추상화 없이 유지보수 가능한 구조를 만든다
* 성능, 감성, UX를 동시에 고려한다
* 첫 사용자 경험이 기분 좋게 끝나도록 구현한다
* 임시 구현보다 완성도 높은 기본형을 우선 만든다

### 하면 안 되는 일

* 명세 범위를 벗어난 기능을 멋대로 추가하지 않는다
* 백엔드, 인증, 데이터 저장, API 연동 같은 불필요한 작업을 하지 않는다
* 3D로 확장하지 않는다
* 과한 게임 요소를 넣지 않는다
* 라이브러리를 많이 붙여서 복잡도를 높이지 않는다
* 구조를 과도하게 일반화하지 않는다

---

## 우선순위

작업은 아래 우선순위대로 진행한다.

### P0 - 핵심 인터랙션 완성

반드시 먼저 완성해야 하는 것:

* 단일 페이지 레이아웃
* 중앙 벚꽃나무 배치
* idle 상태의 미세한 살아있음 표현
* 나무 클릭/터치 인터랙션
* 꽃잎 생성 및 낙하
* 꽃잎 포착 판정
* 결과 모달 표시
* 다시 하기 동작

### P1 - 감성 퀄리티 향상

P0 이후 보강할 것:

* 배경의 미세한 꽃잎 연출
* 모달 애니메이션 정리
* 나무 흔들림 다듬기
* 첫 플레이 성공 UX 강화
* 모바일 터치 영역 최적화

### P2 - 마감 품질

마지막으로 다듬을 것:

* 코드 정리
* 상수 분리
* 타입 정리
* 반응형 마감
* 접근성 보완
* 사운드 on/off
* 공유하기 기능

---

## 권장 기술 스택

### 기본

* React 또는 Vue
* TypeScript
* PixiJS
* GSAP

### 선택

* Howler.js for sound
* Matter.js는 필요할 때만 부분적으로 검토

### 기술 선택 원칙

* 렌더링은 가볍고 안정적이어야 한다
* 꽃잎 낙하는 직접 좌표 계산을 기본으로 한다
* 충돌은 단순 hit test를 우선한다
* “물리엔진을 썼다”보다 “자연스럽고 부드럽다”가 더 중요하다

---

## 작업 방식

### 시작 전

반드시 먼저 할 것:

1. `CHERRY_WISH_SPEC.md` 전체 읽기
2. 상태 머신, 컴포넌트 구조, 설정값 구조 확인
3. 핵심 UX 흐름을 한 줄로 요약해서 머릿속에 고정

핵심 UX 한 줄:
**나무를 누른다 -> 꽃잎이 떨어진다 -> 꽃잎을 잡는다 -> 소원 메시지를 받는다**

### 구현 중

* P0 흐름이 깨지지 않는 선에서만 개선한다
* 기능을 늘리기보다 완성도를 올린다
* 각 단계마다 실제 사용자 관점에서 “이게 바로 이해되는가?”를 판단한다
* 첫 시도 성공률을 반드시 고려한다

### 구현 후

* 모바일에서 터치 경험 확인
* 다시 하기 루프가 안정적인지 확인
* 꽃잎 수와 애니메이션이 과하지 않은지 확인
* 코드 구조가 너무 무겁지 않은지 점검

---

## 디렉터리 원칙

기본 구조는 아래를 따른다.

```text
src/
  app/
    page.tsx 또는 pages/index.tsx
  components/
    CherryWishPage/
    SpringBackground/
    WishTreeScene/
    PetalSystem/
    InstructionOverlay/
    WishResultModal/
    SoundControl/
  constants/
  hooks/
  utils/
  assets/
```

### 디렉터리 규칙

* 페이지 조립과 렌더링 로직을 분리한다
* 상태 관련 로직은 페이지 루트 또는 훅으로 정리한다
* 설정값은 상수 파일로 분리한다
* 계산 로직은 utils 또는 hook으로 분리한다
* 에셋 경로는 명확하게 관리한다

---

## 컴포넌트 책임 분리 원칙

### CherryWishPage

* 전체 상태 머신 관리
* 주요 UI 레이아웃 조립
* 결과 모달 표시 제어
* 다시 하기 흐름 제어

### SpringBackground

* 배경 하늘/빛/느린 꽃잎 연출
* 메인 인터랙션과 강결합하지 않는다

### WishTreeScene

* 나무 렌더링
* 클릭 가능 영역 제어
* 나무 흔들림 실행

### PetalSystem

* 꽃잎 생성
* 프레임 업데이트
* 화면 밖 제거
* 포착 판정
* `wishPetal` 제어

### InstructionOverlay

* 상태에 따른 문구 표시
* 너무 많은 텍스트를 넣지 않는다

### WishResultModal

* 랜덤 메시지 출력
* 다시 하기
* 공유하기

---

## 구현 원칙

### 1. 단순하지만 견고하게

이 프로젝트는 규모가 크지 않다.
따라서 범용 엔진처럼 만들지 말고, 이번 페이지에 최적화된 구조로 만든다.

### 2. 첫 플레이가 가장 중요하다

첫 클릭 후 아무 일도 안 일어나거나, 꽃잎을 못 잡으면 이탈한다.
첫 플레이에서는 다음을 우선 반영한다.

* `wishPetal` 히트박스를 조금 크게
* 속도를 약간 느리게
* 사용자가 놓치지 않도록 시각적으로 약간 더 눈에 띄게

### 3. 감성은 절제해야 한다

예쁜 효과를 많이 넣는 것이 목표가 아니다.
아래는 금지한다.

* 과한 bloom/glow
* 과한 blur/shadow
* 너무 빠른 모션
* 핑크 남용
* 게임 UI 같은 점수/타이머

### 4. 성능은 기능보다 우선이다

꽃잎 수를 늘리기보다 부드러움을 확보한다.
모바일에서 프레임 드랍이 보이면 꽃잎 수를 줄이는 쪽을 택한다.

---

## 상태 머신 원칙

다음 상태를 유지한다.

```ts
export type SceneState =
  | 'idle'
  | 'shaking'
  | 'petalsFalling'
  | 'petalCaught'
  | 'resultOpen'
  | 'resetting'
```

### 상태 전이 원칙

* 불필요한 상태 추가 금지
* 상태 이름은 의미가 명확해야 한다
* 애니메이션 중복 실행을 막는다
* `resetting` 이후 반드시 `idle`로 안정 복귀해야 한다

---

## 꽃잎 엔진 원칙

### 기본 전략

꽃잎은 개별 물리 바디보다 **시뮬레이션 객체**로 관리한다.

권장 필드 예시:

* id
* x, y
* vx, vy
* rotation
* rotationSpeed
* driftPhase
* driftAmplitude
* scale
* opacity
* isWishPetal
* isActive

### 업데이트 원칙

매 프레임 다음을 업데이트한다.

* 위치
* 속도
* 드리프트
* 회전
* 범위 밖 제거

### 충돌 원칙

* 사각형 또는 원형 히트 테스트 우선
* `wishPetal`은 일반 꽃잎보다 판정 반경을 크게 할 수 있다
* 불필요한 충돌 연산 금지

---

## 애니메이션 원칙

### 나무

* 클릭 직후 0.8~1.2초 정도 흔들림
* GSAP으로 자연스러운 탄성 표현
* 과장된 흔들림 금지

### 꽃잎

* 살랑살랑 떨어져야 한다
* 중력은 약하게
* 좌우 드리프트가 있어야 한다
* 모든 꽃잎이 똑같이 움직이면 안 된다

### 모달

* 페이드 + 가벼운 확대
* 너무 무겁거나 튀지 않게
* 결과 경험을 해치지 않도록 빠르게 등장

---

## 스타일 가이드

### 전체 무드

* 조용한 봄날
* 부드럽고 따뜻한 감성
* 과하지 않은 세련됨

### 시각적 우선순위

1. 나무
2. 꽃잎
3. 결과 메시지
4. 배경 장식

### 스타일 금지사항

* 진한 핫핑크 위주 구성
* 무거운 입체감
* 지나치게 장식적인 타이포
* 복잡한 버튼 시스템

---

## 코드 품질 기준

### 필수

* TypeScript strict 기준으로 오류 없이 작성
* 명확한 타입 선언
* 함수/변수명은 역할이 드러나게 작성
* 상수 분리
* 컴포넌트 책임 분리

### 지양

* 하드코딩 남발
* 한 파일에 모든 로직 몰아넣기
* 추상화만 많고 읽기 어려운 구조
* 사용하지 않는 유틸/훅 생성

### 주석 원칙

* 왜 필요한지 설명하는 주석만 남긴다
* 코드 자체로 드러나는 내용은 주석으로 반복하지 않는다

---

## 체크리스트

### 구현 전 체크

* [ ] `CHERRY_WISH_SPEC.md`를 읽었다
* [ ] 단일 화면 구조를 이해했다
* [ ] 상태 머신 구조를 이해했다
* [ ] 우선순위가 P0 중심으로 잡혀 있다

### 구현 중 체크

* [ ] 나무 클릭 시 즉시 반응한다
* [ ] 꽃잎 낙하가 자연스럽다
* [ ] 첫 플레이가 지나치게 어렵지 않다
* [ ] 모바일에서도 클릭/터치가 쉽다
* [ ] 다시 하기 루프가 정상 동작한다

### 구현 후 체크

* [ ] idle 상태가 살아 있다
* [ ] petalsFalling 상태에서 프레임 드랍이 심하지 않다
* [ ] result modal 진입이 자연스럽다
* [ ] 상수/타입/컴포넌트가 분리되어 있다
* [ ] 디자인이 과하지 않다

---

## 완료 기준

다음 조건을 만족하면 구현 완료로 본다.

1. 진입 직후 나무와 배경이 미세하게 살아 있다
2. 나무 클릭 시 즉시 흔들림과 꽃잎 낙하가 시작된다
3. 사용자가 꽃잎을 잡을 수 있다
4. 결과 메시지 모달이 자연스럽게 뜬다
5. 다시 하기 후 동일한 흐름을 안정적으로 반복할 수 있다
6. 모바일에서도 핵심 체험이 끊기지 않는다
7. 전체 코드가 과하지 않게 구조화되어 있다
8. 결과물이 감성적이되 촌스럽지 않다

---

## 최종 작업 지시

이 프로젝트에서 가장 중요한 것은 기능 수가 아니라 **짧고 선명한 체험의 완성도**다.
에이전트는 새로운 기능을 늘리기보다, 아래 경험을 가장 아름답게 완성하는 데 집중해야 한다.

**나무를 누른다 -> 꽃잎이 떨어진다 -> 꽃잎을 잡는다 -> 소원 메시지를 받는다**

이 흐름이 빠르고, 직관적이고, 자연스럽고, 기분 좋게 느껴지면 성공이다.
